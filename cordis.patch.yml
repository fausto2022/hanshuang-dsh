# cordis.patch.yml — 牛来插件注入 DSH 插件树
# 不要写 group: system-prompt。group 在 Cordis 里表示「自己是嵌套组」，
# 不是「加入某个组」；写上后段落会挂到错误作用域，Agent 看不到牛来提示词。
- insert:
    - id: dsh-niulai
      name: dsh-niulai
      config:
        promptVariant: v3
        activationWord: 牛来
        activationResponse: 已成功
