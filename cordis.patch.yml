# cordis.patch.yml — 寒霜插件注入 DSH 插件树
# 不要写 group: system-prompt。group 在 Cordis 里表示「自己是嵌套组」，
# 不是「加入某个组」；写上后段落会挂到错误作用域，Agent 看不到寒霜提示词。
- insert:
    - id: dsh-hanshuang
      name: dsh-hanshuang
      config:
        promptVariant: v3
        activationWord: 寒霜
        activationResponse: 已成功
